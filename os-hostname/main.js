// Importamos el módulo 'os' de Node.js, que proporciona funciones para obtener información del sistema operativo.
const os = require('node:os');  

// Llamamos a la función `hostname()` del módulo 'os', 
// la cual devuelve el nombre del host (nombre del equipo o computadora).
console.log(os.hostname());  
